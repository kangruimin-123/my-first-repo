# 主线龙头交易系统 — 工程方案 v4.2 + Codex 任务拆解

> **版本**：v4.2 · 2026-05-16
> **定位**：个人 A 股主线龙头交易辅助系统
> **执行模式**：AI 主导开发，人辅助验收
> **v4.1 → v4.2 修正**：
> 1. "17 步"改为"21 步完整策略管线"
> 2. 候选池拆为观察池 ≤50 + 交易重点池 ≤10
> 3. 竞价数据不可用时自动跳过，不阻塞 daily
> 4. positions 改 manual_positions，禁止自动同步券商
> 5. 补 LLM 输入上下文要求

---

# 第一部分：工程方案

---

## 〇、核心设计原则

```
1. 先搭执行管线和数据闭环，再逐步填充策略复杂度
2. 每个策略第一版允许简化实现，但入口、配置、输出格式必须完整保留
3. 不要一次性实现 21 个策略的完整复杂逻辑
4. 所有外部数据先写入 SQLite，策略层只从 SQLite 读
5. 服务重启后从 SQLite 恢复状态，不拉全量数据
6. 数据源三级降级，三级全败才报错
7. deny_check 和 stop_loss 是硬规则，永远不被跳过
8. 竞价/盘中策略在数据不可用时自动跳过，不阻塞 daily 主流程
9. 持仓数据只允许手工录入，禁止自动同步券商账户
```

---

## 一、数据架构

### 1.1 数据源降级链

```
┌──────────────┬──────────────┬───────────────┬──────────────┐
│ 数据类型      │ Level 1 首选  │ Level 2 降级   │ Level 3 兜底  │
├──────────────┼──────────────┼───────────────┼──────────────┤
│ 个股日线      │ Tushare daily│ akshare       │ SQLite 缓存   │
│ 指数日线      │ Tushare      │ akshare       │ SQLite 缓存   │
│ 涨停/连板     │ Tushare      │ akshare       │ SQLite 缓存   │
│ 板块/概念     │ Tushare      │ akshare       │ SQLite 缓存   │
│ 个股基础      │ Tushare      │ akshare       │ SQLite 缓存   │
│ 资金流向      │ Tushare      │ 跳过(非必需)   │ 空值标记      │
│ 竞价快照      │ efinance     │ Tushare       │ mock         │
│ 盘中实时      │ efinance     │ Tushare       │ 最近收盘价    │
│ 分时成交      │ efinance     │ mock          │ 空值标记      │
│ Level2       │ mock         │ mock          │ mock         │
└──────────────┴──────────────┴───────────────┴──────────────┘

竞价/盘中数据不可用时：
  → 相关策略自动跳过（enabled 但 data unavailable → skip + log）
  → 不阻塞 daily 主流程
  → 受影响信号标记 data_quality: degraded
```

### 1.2 持久化：SQLite 单库

```
trading_system.db
│
├─ market_data ─────────────────────────────
│  daily_kline          symbol + date 去重，增量更新
│  index_kline          指数日线
│  daily_basic          市值/换手/PE
│  limit_up_records     涨停记录
│  lianban_records      连板统计
│  sector_mapping       个股→板块（7天过期刷新）
│  sector_daily         板块日度汇总
│  moneyflow            资金流（可空）
│  auction_snapshot     竞价快照（Phase 4）
│  intraday_snapshot    盘中快照（Phase 5）
│
├─ strategy_data ────────────────────────────
│  mainline_history     主线识别历史
│  role_assignment      角色识别结果
│  evaluation_results   每日评估结果
│  strategy_signals     信号明细
│
├─ portfolio_data ───────────────────────────
│  manual_positions     手工持仓（禁止自动同步券商）
│  trade_history        交易历史（手工记录）
│  stop_loss_levels     止损位
│
├─ radar_data（Phase 7 预留）──────────────
│  mainline_radar      板块异动记录 date + sector + score + reason
│  leader_radar        潜在龙头预标记 date + symbol + probability + reason
│  risk_radar          衰减预警 date + symbol/sector + level + reason
│
└─ system
   system_meta          last_update / data_source_status / degradation_log
```

### 1.3 增量更新

```
⚠ 性能关键约束：禁止逐只拉日线，必须用按日期批量接口。
  Tushare daily(trade_date=) → 一次返回全市场当天 ~3500 行
  120 天 = 120 次调用 ≈ 36 秒（正确）
  3500 只 × 逐只 = 3500 次 ≈ 17 分钟（错误，且可能被封）

首次全量同步（120 天）：< 10 分钟
每日增量同步：< 30 秒
全市场分析（3500 只）：< 10 秒
策略执行（观察池 50 只）：< 5 秒

启动时 → 读 last_daily_update → 今天则跳过 → 否则增量拉取
交易日 15:05 → 拉日线/涨停/连板/板块汇总 → 写 SQLite → 更新 meta
保留 180 天，超期清理
每日备份 db 文件
```

### 1.4 数据质量标记

```
full      所有数据源正常
degraded  部分降级，信号最高 B 档
partial   关键缺失，仅供参考
mock      模拟数据，仅开发测试
```

---

## 二、股票池范围

### 2.1 日线扫描池

```
范围：A 股全市场
排除：ST/*ST / 北交所 / 上市<60天 / 5日均额<5000万 / 停牌
规模：约 3000~3500 只
用途：mainline_detect / leader_detect
```

### 2.2 今日观察池

```
来源：日线扫描池 → mainline_detect → role_detect → 策略过滤
规模：Top5 主线 × 每板块 ≤10 = ≤50 只
用途：全部策略评估、买入分档
```

### 2.3 今日交易重点池

```
来源：观察池 → 策略评分 + 风控 → 取 Top
规模：≤10 只
用途：你每天实际关注和操作的标的
显示：终端输出和 HTML 报告的"重点关注"区域
```

### 2.4 盘中实时监控池

```
范围：今日交易重点池 + 手工持仓 + 手动查询
规模：≤30 只
```

### 2.5 预备观察池（雷达输出，Phase 7）

```
来源：Radar Engine 输出
  - 主线雷达：Top6-20 板块中的异动板块
  - 龙头雷达：板块启动前预标记的潜在龙头
  - 退潮雷达：当前持仓/观察池中的衰减预警
规模：≤20 只
用途：明日重点观察，今日不操作
行为：不触发买入策略，仅展示在报告"明日关注"区域
```

### 2.6 手动自选池（可选）

```
文件：sample_data/watchlist.csv
行为：走全部策略但标记 is_watchlist=true，不影响主线评分
```

### 股票池完整流转

```
扫描池（3500）
  ↓
机会雷达 ────→ 预备观察池（明日关注 ≤20）  ← Phase 7
  ↓
观察池（今日可操作 ≤50）
  ↓
交易重点池（≤10）
  ↓
盘中池（≤30）
```

---

## 三、策略架构

### 3.1 完整策略管线（21 步 + 雷达前置）

```
 0. radar_scan（雷达扫描）          ← Phase 7（预备观察池，不影响主链路）

 1. mainline_detect                ← Phase 2
 2. mainline_switch                ← Phase 4
2.5. stage_analyze                 ← Phase 6（阶段识别，在角色之前）
 3. leader_detect                  ← Phase 2
 4. core_mid_detect                ← Phase 2
 5. elastic_detect                 ← Phase 2
 6. lianban_leader_template        ← Phase 4
 7. auction_relative_strength      ← Phase 4
 8. deny_check                     ← Phase 3（最高优先级，含阶段风险）
 9. leader_pullback                ← Phase 3（受阶段门控）
10. leader_breakout                ← Phase 3
11. leader_first_divergence        ← Phase 4
12. leader_trend_continue          ← Phase 4
13. core_mid_trend_pullback        ← Phase 3
14. elastic_breakout               ← Phase 5
15. panic_reversal                 ← Phase 4
16. position_control               ← Phase 3
17. stop_loss                      ← Phase 3
18. leader_reseal                  ← Phase 5
19. trend_hold                     ← Phase 4
20. llm_review                     ← Phase 5
21. signal_eval                    ← Phase 3

Phase 2-3 启用的步骤：1, 3, 4, 5, 8, 9, 10, 13, 16, 17, 21（共 11 步）
其余步骤按 Phase 逐步解锁
未解锁 / 数据不可用的步骤在管线中自动跳过
```

### 3.2 策略分级交付

```
MVP 必做（11 个）                         ← Phase 2-3
  mainline_detect / leader_detect / core_mid_detect / elastic_detect
  deny_check / leader_pullback / leader_breakout / core_mid_trend_pullback
  position_control / stop_loss / signal_eval

第二批（7 个）                             ← Phase 4
  mainline_switch / auction_relative_strength / lianban_leader_template
  leader_first_divergence / leader_trend_continue / trend_hold / panic_reversal

第三批（4 个）                             ← Phase 5
  leader_pullback(分时版) / leader_reseal / elastic_breakout(盘中版) / llm_review
```

### 3.3 输出格式

```python
@dataclass
class StrategySignal:
    strategy_name: str
    symbol: str
    action: str                # buy / sell / hold / watch / deny / add / reduce
    confidence: float          # 0-1
    reason: str
    action_text: str           # 人话操作建议
    data_quality: str          # full / degraded / partial / mock
    entry_price_low: float     # 买入区间下限（无则 0）
    entry_price_high: float    # 上限（无则 0）
    stop_loss_price: float     # 止损价（无则 0）
    position_pct: float        # 建议仓位
    grade: str                 # A / B / C / NONE
    sell_urgency: str          # 清仓 / 减仓 / 防守 / 无
    risk_warnings: list[str]
    timestamp: datetime
```

### 3.4 买入分档

```
A 档（确认买点，可执行）：
  全部满足：
  ✓ 板块在 Top5 主线
  ✓ deny_check 通过
  ✓ 至少一个买入策略命中
  ✓ data_quality == full
  ✓ position_control 允许
  ✓ 有明确止损位
  ✓ confidence >= 0.6

B 档（临界观察）：
  - 板块 Top6~10 / data_quality == degraded / confidence 0.4~0.6 / deny_check == watch

C 档（仅保留）：
  - 主线内无策略触发 / 有 warning / partial 或 mock
```

### 3.5 卖出四级

```
清仓：跌破 MA60 向下 + 无反弹 → 不接受降级保护
      market_regime == weak 时减仓升级为清仓
减仓：跌破 MA20 放量 / 板块退潮初期 / 弱于板块 3 天
防守：接近止损位 / 板块走弱未退潮 / 放量长上影
无：  不满足以上

强势股保护：仅对减仓和防守生效，清仓不降级
```

### 3.6 LLM 输入上下文要求（Phase 5）

```
llm_review 的输入必须包含以下完整上下文，不允许只传价格：

必传字段：
  - 板块强度（sector_score + mainline_status）
  - 板块在 Top N 中的排名
  - 个股角色（leader / core_mid / elastic）
  - 近 5 日走势摘要（涨跌幅 + 量能变化）
  - 近 10 日走势摘要
  - 成交额变化趋势
  - 换手率变化趋势
  - 策略命中结果（哪些策略触发了什么信号）
  - 风控结果（deny_check 输出）
  - 当前持仓状态（如有）

可选字段（有则传）：
  - 竞价数据（auction_score + status）
  - 实时行情（盘中模式）
  - 分时摘要（VWAP 位置、承接情况）

LLM 硬规则：
  - LLM 看多但风控禁买 → 仍然禁买
  - LLM 不能生成自动下单建议
  - LLM 不能覆盖止损
  - LLM 失败不影响主链路
```

---

## 四、工程架构

### 4.1 目录结构

```
trading-system/
├── run.py
├── config.yaml
├── requirements.txt
├── README.md
├── trading_system.db                   # 运行时生成
│
├── backend/
│   ├── __init__.py
│   ├── config.py
│   ├── db.py                           # SQLite ORM + 表定义
│   │
│   ├── data/
│   │   ├── __init__.py
│   │   ├── provider_base.py            # 抽象基类 + 降级装饰器
│   │   ├── tushare_provider.py
│   │   ├── efinance_provider.py        # Phase 4+
│   │   ├── akshare_provider.py
│   │   ├── mock_provider.py
│   │   ├── cache_manager.py            # SQLite 读写
│   │   ├── data_sync.py                # 同步调度
│   │   ├── degradation.py              # 降级链
│   │   └── stock_pool_filter.py        # 全市场过滤
│   │
│   ├── analysis/
│   │   ├── __init__.py
│   │   ├── mainline_analyzer.py
│   │   ├── role_analyzer.py
│   │   ├── trend_analyzer.py
│   │   ├── position_analyzer.py
│   │   ├── volume_price_analyzer.py
│   │   ├── market_risk_analyzer.py
│   │   ├── stage_analyzer.py           # Phase 6
│   │   ├── dow_analyzer.py             # Phase 6
│   │   ├── chart_pattern_analyzer.py   # Phase 6
│   │   ├── chip_analyzer.py            # Phase 6（预留）
│   │   ├── wave_analyzer.py            # Phase 6（预留）
│   │   ├── mainline_radar.py           # Phase 7（预留）
│   │   ├── leader_radar.py             # Phase 7（预留）
│   │   └── risk_radar.py               # Phase 7（预留）
│   │
│   ├── strategy/
│   │   ├── __init__.py
│   │   ├── base_strategy.py
│   │   ├── mainline_strategy.py
│   │   ├── leader_detect_strategy.py
│   │   ├── leader_trade_strategy.py
│   │   ├── mid_trend_strategy.py
│   │   ├── elastic_strategy.py
│   │   ├── auction_strategy.py         # Phase 4
│   │   ├── lianban_strategy.py         # Phase 4
│   │   ├── risk_rules.py
│   │   └── signal_eval_strategy.py
│   │
│   ├── llm/                            # Phase 5
│   │   ├── __init__.py
│   │   ├── llm_client.py
│   │   ├── llm_review.py
│   │   └── prompts.py
│   │
│   ├── engine/
│   │   ├── __init__.py
│   │   ├── pipeline.py                 # 21 步管线（未解锁自动跳过）
│   │   ├── grading.py                  # 分档 + 交易重点池筛选
│   │   ├── daily_runner.py
│   │   ├── auction_runner.py           # Phase 4
│   │   └── intraday_runner.py          # Phase 5
│   │
│   └── output/
│       ├── __init__.py
│       ├── terminal_view.py
│       ├── csv_reporter.py
│       └── html_reporter.py
│
├── tests/
│   ├── conftest.py
│   ├── test_db.py
│   ├── test_data_providers.py
│   ├── test_degradation.py
│   ├── test_stock_pool_filter.py
│   ├── test_mainline.py
│   ├── test_role_detect.py
│   ├── test_analysis.py
│   ├── test_strategies.py
│   ├── test_risk_rules.py
│   ├── test_grading.py
│   ├── test_pipeline.py
│   └── test_signal_eval.py
│
├── sample_data/
│   ├── watchlist.csv
│   └── mock_kline/
│
├── backups/
└── output/
    └── {YYYYMMDD}/
```

### 4.2 运行模式

```bash
python run.py --mode sync              # 只拉数据
python run.py --mode daily             # 收盘后主流程
python run.py --mode auction           # 竞价阶段（Phase 4）
python run.py --mode intraday -i 30    # 盘中循环（Phase 5）
python run.py --mode detail -s 002415.SZ
python run.py --mode eval --days 60    # 信号回验
python run.py --mode status            # 系统状态
```

---

## 五、Phase 规划

```
Phase 1 ─ 地基           Task 00-02    数据+持久化+降级+过滤
Phase 2 ─ 主线与角色      Task 03-04    mainline+角色+分析器
Phase 3 ─ 核心策略闭环    Task 05-07    策略+风控+分档+输出+eval
Phase 4 ─ 竞价增强        Task 08-09    竞价+连板+补充策略
Phase 5 ─ 盘中+LLM       Task 10-11    实时+LLM+完善
Phase 6 ─ 阶段增强        Task 12-14    Stage Engine+策略门控+eval升级
Phase 7 ─ 机会雷达        Task 15-17    主线雷达+龙头雷达+退潮雷达（数据驱动决定）
```

### Phase 6 说明

v4.2 策略链路缺失"个股所处阶段"判断，导致可能在派发期给出买点。
Phase 6 新增 Stage Engine（阶段引擎），升级后链路：

```
主线 → 阶段识别 → 角色 → 策略（受阶段门控）→ 风控
```

五阶段：吸筹期 → 启动期 → 主升期 → 派发期 → 退潮期
第一版实做：道氏趋势(45%) + 图表结构(30%) + 量价(25%)
筹码 + 波浪：预留接口，第一版 unknown，权重自动重分配

策略门控：每个策略配置 allowed_stages，阶段不允许则自动 deny
deny_check 升级：Stage 3 风险 +30，Stage 4 直接 deny

### Phase 7 说明（数据驱动决定何时启动）

Phase 1-3 跑通后，用 signal_eval 数据回答：
  mainline_detect 识别到 Top5 时距离板块真正启动滞后几天？
  → 滞后 1 天：雷达边际价值小，优先级降低
  → 滞后 3+ 天：必须做雷达

核心理念：不做预测，做雷达。
  不预测明天涨什么，而是提前发现机会 + 提前发现危险 + 提前准备剧本。

三层雷达：
  主线雷达：Top6-20 异动板块（量先于价）× 周期过滤
  龙头雷达：板块启动前预标记潜在龙头（辨识度+题材纯度+筹码成熟度+Stage 0/1）
  退潮雷达：龙头衰减 + 板块衰减 + 周期结束预警

关键约束：
  雷达 × 阶段 = 交叉验证，Stage 是中枢
  异动 + Stage 4 → 直接降权（骗炮）
  异动 + Stage 0/1 → 高价值信号

---

## 六、config.yaml

```yaml
system:
  name: "主线龙头交易系统"
  version: "4.2.0"
  timezone: "Asia/Shanghai"
  log_level: "INFO"
  db_path: "trading_system.db"
  backup_dir: "backups"
  data_retention_days: 180

data_source:
  daily:
    chain: ["tushare", "akshare", "cache"]
  auction:
    chain: ["efinance", "tushare", "mock"]
  intraday:
    chain: ["efinance", "tushare", "mock"]
  level2:
    chain: ["mock"]
  tushare:
    token: ""
    request_interval: 0.3
    retry_times: 3
    history_days: 120
  cache:
    expire_hours: 12
    sector_expire_days: 7

stock_pool:
  exclude_st: true
  exclude_bse: true
  min_list_days: 60
  min_avg_amount_5d: 50000000
  exclude_suspended: true
  watchlist_path: "sample_data/watchlist.csv"
  max_observation_pool: 50
  max_focus_pool: 10

mainline:
  top_n: 5
  score_weights:
    sector_pct_chg: 0.20
    sector_amount: 0.15
    limit_up_count: 0.20
    lianban_count: 0.15
    leader_strength: 0.15
    duration: 0.10
    money_flow: 0.05
  status_thresholds:
    rising_min_score: 60
    fading_max_score: 30
    rotation_volatility: 0.5
  switch_threshold: 0.3

role_detect:
  leader:
    min_amount_rank_pct: 0.2
    min_pct_chg_rank_pct: 0.2
  core_mid:
    min_market_cap: 100
    require_ma20_up: true
    require_ma60_up: true
  elastic:
    max_market_cap: 100
    min_turnover: 5.0

strategies:
  leader_pullback:
    enabled: true
    ma_support: 20
    pullback_max_pct: 5.0
    require_volume_shrink: true
    min_sector_score: 50
    position_pct: [0.10, 0.20]
  leader_breakout:
    enabled: true
    breakout_window: 20
    volume_ratio_min: 1.5
    pct_change_min: 1.5
    close_position_min: 0.7
    require_sector_sync: true
    position_pct: [0.10, 0.15]
  leader_first_divergence:
    enabled: false
    max_drop_from_high: 10.0
    require_mainline_active: true
    position_pct: [0.10, 0.15]
  leader_trend_continue:
    enabled: false
    require_ma20_hold: true
  leader_reseal:
    enabled: false
    require_realtime: true
  core_mid_trend_pullback:
    enabled: true
    ma_support: 20
    require_ma60_up: true
    position_pct: [0.20, 0.30]
  elastic_breakout:
    enabled: false
    require_mainline: true
    max_position_pct: 0.10
  panic_reversal:
    enabled: false
    max_position_pct: 0.10
    require_leader_intact: true
  auction_relative_strength:
    enabled: false
    require_realtime_data: true
    skip_if_data_unavailable: true
    compare_with_sector: true
    compare_with_lianban_group: true
  lianban_leader_template:
    enabled: false
    require_mainline: true
    min_lianban_count: 2
    require_auction_strength: true
    skip_if_data_unavailable: true
    deny_back_row: true
    deny_overheated_high_open: true
    max_position_pct: 0.10
  mainline_switch:
    enabled: false
    score_diff_threshold: 0.3
  trend_hold:
    enabled: false

risk:
  deny_check:
    non_mainline_deny: true
    back_row_deny: true
    overheated_deny: true
    no_stop_loss_deny: true
    min_risk_reward_ratio: 2.0
  position_control:
    leader_pullback: [0.10, 0.20]
    lianban_leader: [0.05, 0.15]
    core_mid_trend: [0.20, 0.30]
    elastic: [0.05, 0.10]
    panic_reversal: [0.00, 0.10]
    max_total_position: 0.80
    weak_market_max: 0.20
  stop_loss:
    default_pct: 5.0
    use_ma20: true
    use_prev_low: true
  sell_rules:
    strong_stock_protection: true
    clear_signal_no_downgrade: true

grading:
  a_grade:
    require_top5_mainline: true
    require_deny_pass: true
    require_full_data: true
    min_confidence: 0.6
    require_stop_loss: true
  b_grade:
    allow_top10_mainline: true
    allow_degraded_data: true
    min_confidence: 0.4

llm:
  enabled: false
  provider: "openrouter"
  model: "anthropic/claude-3-haiku"
  api_key: ""
  timeout: 10
  fallback_on_error: true

output:
  terminal: true
  csv: true
  html: true

signal_eval:
  eval_windows: [1, 3, 5]
  max_drawdown_window: 5
```

---

## 七、风险与兜底

| 风险 | 兜底 |
|---|---|
| Tushare 不稳定 | akshare → SQLite 缓存 |
| efinance 变动 | Tushare → mock，标记 degraded |
| 竞价数据全部降级 | 竞价策略自动跳过，不阻塞 daily |
| 主线识别不准 | Phase 2 人工验收，调 score_weights |
| 假阳性过高 | signal_eval 监控，胜率<40% 关闭策略 |
| LLM 不可用 | 默认关闭，不影响规则 |
| SQLite 损坏 | 每日自动备份 |
| 数据量增长 | 180 天清理；未来迁 DuckDB |

---

## 八、技术选型

```
Python 3.11+ / SQLite + SQLAlchemy 2.0
tushare / efinance / akshare
pandas + numpy / rich / httpx
APScheduler / pyyaml / pytest
```
