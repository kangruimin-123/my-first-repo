# 主线龙头交易系统 — 前端设计提示词

> 用途：交给 AI Studio（Firebase/Gemini）或 Cursor 等 AI 工具生成前端代码
> 后端接口：Python backend 通过 JSON 文件 / REST API / WebSocket 提供数据
> 部署：本地运行，浏览器访问 localhost

---

## 提示词（直接复制给 AI）

---

我需要你为一个 **个人 A 股主线龙头交易辅助系统** 设计并实现完整的前端。这不是一个通用的股票行情软件，而是一个 **专业短线交易者的每日决策仪表盘**。用户只有一个人（我自己），每天盘前、盘中、盘后各看一次。

### 产品形态

单页应用（SPA），左侧导航 + 主内容区 + 右侧面板。深色主题为主（交易者长时间盯屏，深色护眼），支持一键切换亮色。本地部署，浏览器全屏使用。

### 设计方向

**"作战指挥室"风格**——不是彭博终端的密集信息流，也不是消费级 App 的圆角卡片。而是介于两者之间的 **精密工具感**：信息密度高但层次清晰，像战斗机座舱的 HUD 而不是手机 App。

具体风格：
- **色调**：深灰/近黑底（#0a0a0f ~ #12121a），辅以冷蓝色强调线（#3b82f6）和战略橙（#f59e0b）作为警告色。A 档信号用翠绿（#10b981），卖出信号用珊瑚红（#ef4444）。
- **字体**：等宽数字字体（JetBrains Mono 或 IBM Plex Mono）用于价格和数据，无衬线字体（DM Sans 或 Outfit）用于标签和说明。中文用 Noto Sans SC。
- **布局**：网格化但不死板，关键信息有呼吸感。用细线（1px, rgba 白色 10%）分割区域，不用粗边框。
- **动效**：数据刷新时的淡入，信号出现时的脉冲光晕，预警的缓慢闪烁。不要花哨过渡，要精准、克制。
- **特征元素**：左上角状态灯（绿=正常/黄=降级/红=异常），卡片左侧的 2px 色带标识类型，hover 时展开详情的微交互。

### 技术栈

- React 18+ / TypeScript
- TailwindCSS
- Recharts 或 Lightweight Charts（K 线图）
- Framer Motion（动效）
- 数据通过 JSON 文件加载（后端生成 evaluation.json / radar.json / status.json）
- 后续可升级为 WebSocket 实时推送

### 页面结构（共 5 个视图）

---

#### 视图 1：每日总览（默认首页）

这是核心页面，盘后看。布局：

```
┌─────────────────────────────────────────────────────────────┐
│ 顶栏：系统名 + 日期 + 数据状态灯 + 大盘环境标签 + 模式切换  │
├────────┬────────────────────────────────────────────────────┤
│        │ ┌──────────────────────────────────────────────┐   │
│ 左侧   │ │  大盘环境卡片                                 │   │
│ 导航   │ │  regime: risk_on/neutral/weak                │   │
│        │ │  涨跌比 + 指数位置 + Top5 主线一览             │   │
│ 总览   │ └──────────────────────────────────────────────┘   │
│ 雷达   │                                                    │
│ 持仓   │ ┌──────────────────────────────────────────────┐   │
│ 回测   │ │  交易重点池（≤10 只）                          │   │
│ 设置   │ │  这是最核心的区域，要最突出                      │   │
│        │ │                                              │   │
│        │ │  每只股票一行：                                │   │
│        │ │  [A/B 色带] 名称 代码 现价 涨跌幅              │   │
│        │ │  策略标签 阶段标签 买入区间 止损位              │   │
│        │ │  操作建议（人话）                               │   │
│        │ │  展开：详细分析 + 风控结果                      │   │
│        │ └──────────────────────────────────────────────┘   │
│        │                                                    │
│        │ ┌──────────────┐  ┌──────────────────────────┐    │
│        │ │ 卖出预警      │  │ 阶段拦截（被 deny 的）    │    │
│        │ │ 红色主题      │  │ 灰色主题                  │    │
│        │ └──────────────┘  └──────────────────────────┘    │
│        │                                                    │
│        │ ┌──────────────────────────────────────────────┐   │
│        │ │  完整观察池（折叠，点击展开）                    │   │
│        │ └──────────────────────────────────────────────┘   │
├────────┴────────────────────────────────────────────────────┤
│ 底栏：最后同步时间 + 降级状态 + 数据质量                      │
└─────────────────────────────────────────────────────────────┘
```

**交易重点池**是最重要的组件，设计要求：
- 每只股票是一个可展开的卡片
- 左侧 2px 色带：A 档翠绿 / B 档琥珀 / 卖出珊瑚红
- 阶段标签（stage_1 启动 / stage_2 主升 / stage_3 派发）用不同颜色小标签
- 策略标签（leader_pullback / leader_breakout 等）用 outline 标签
- 买入区间显示为一个小价格条（低→高），止损位用红色虚线标注
- 展开后显示：趋势分析 / 量价 / 板块强度 / deny_check 结果 / LLM 解释（如有）
- 数据质量标记：full 不显示，degraded 显示黄色小标签，mock 显示灰色

**大盘环境卡片**：
- risk_on 绿色调 + "进攻" 标签
- neutral 蓝色调 + "中性" 标签  
- weak 红色调 + "防守" 标签
- 包含：涨跌比环形图 + 指数 vs MA20 位置

**Top5 主线一览**：
- 横向 5 个小卡片，每个显示板块名 + mainline_score + status 标签
- status 颜色：rising 绿 / continuing 蓝 / rotation 橙 / fading 红

---

#### 视图 2：机会雷达

盘前看。展示预备观察池 + 退潮预警。

```
上半区：明日关注
  - 主线雷达命中的异动板块（卡片式）
  - 每个板块卡片包含：radar_score / 信号类型 / 周期阶段 / 建议关注的潜在龙头
  - 板块卡片内嵌小表格：该板块 Top3 潜在龙头 + leader_probability

下半区：退潮预警
  - 三列：danger / caution / watch
  - 每列用对应颜色主题
  - 每条预警：标的 + 信号类型 + 原因 + 建议动作
```

---

#### 视图 3：持仓管理

展示 manual_positions + 止损监控。

```
持仓列表：
  每只持仓显示：
  - 股票名称 代码
  - 买入价 → 现价（涨跌幅 + 颜色）
  - 止损位（红色线）+ 距止损百分比
  - 持仓天数
  - 当前阶段（Stage 标签）
  - 趋势状态 + hold/reduce/sell 建议
  - 盈亏金额和百分比

右侧面板：
  - 总仓位饼图（已用 / 剩余）
  - 大盘环境对仓位的约束提示
  - 持仓的板块分布

底部：
  - 手动添加持仓按钮（symbol + 买入价 + 数量 + 止损 + 备注）
```

---

#### 视图 4：信号回测

展示 signal_eval 的统计数据。

```
顶部：总览卡片
  - 总信号数 / 总胜率 / 平均收益率 / 最大回撤

中部：按策略分组表格
  - 每个策略一行：信号数 / 1日胜率 / 3日胜率 / 5日胜率 / 平均收益
  - 按胜率排序，胜率 > 55% 绿色高亮，< 45% 红色高亮

新增：按阶段分组表格
  - stage_1 / stage_2 的信号表现
  - 被阶段拦截的信号"假设表现"

底部：门控效果卡片
  - 被风控拦截的信号假设表现 vs 通过的信号表现
  - 如果拦截有效（拦截的表现差），显示绿色 "✓ 风控有效"
  - 如果拦截无效（拦截的反而好），显示橙色 "⚠ 门控可能过严"

图表：
  - 按时间的信号表现折线图（Recharts）
  - 各策略的胜率雷达图
```

---

#### 视图 5：系统设置

```
数据源状态：
  - 每个数据源一行：名称 + 当前状态（正常/降级/不可用）+ 最后成功时间
  - 降级链可视化：Tushare → akshare → SQLite

性能指标：
  - 最后同步耗时
  - 数据库大小
  - 各表行数

快捷操作：
  - 手动触发同步
  - 清理过期数据
  - 查看降级日志
```

---

### 关键组件设计

#### 股票卡片（最核心的组件）

```
┌─ 2px 色带 ─────────────────────────────────────────┐
│  🟢 A档                                             │
│  海康威视  002415.SZ               ¥38.50  +2.35%   │
│                                                     │
│  ┌─────────┐ ┌────────────┐ ┌──────────┐           │
│  │ stage_2  │ │ breakout   │ │ leader   │           │
│  │ 主升期   │ │ 龙头突破    │ │ 龙头     │           │
│  └─────────┘ └────────────┘ └──────────┘           │
│                                                     │
│  买入区间 ━━━━━━━━━━━━━ ¥37.50 ~ ¥38.80            │
│  止损位   ╌╌╌╌╌╌╌╌╌╌╌╌ ¥36.50                      │
│  建议仓位  15%                                       │
│                                                     │
│  💬 "突破确认，板块共振，可半仓介入"                    │
│                                                     │
│  ▼ 展开详情                                         │
├─────────────────────────────────────────────────────┤
│  趋势：strong_up ▲  均线多头排列  slope: +0.032     │
│  量价：volume_ratio 1.8x  放量突破  ✓               │
│  板块：AI算力 Top1  score: 82  status: rising       │
│  风控：deny_check ✓  risk_reward: 3.2              │
│  LLM："分歧后放量突破，主升延续概率高"               │
│  数据质量：full ✓                                    │
└─────────────────────────────────────────────────────┘
```

#### 退潮预警卡片

```
┌─ 2px 红色带 ────────────────────────────────────────┐
│  ⚠ danger                                           │
│  海康威视  002415.SZ                                 │
│                                                     │
│  信号：高位放量阴线                                    │
│  原因：volume_ratio 2.3x + 跌幅 -3.2% + 高位        │
│  建议：准备止损                                       │
│                                                     │
│  ┌───────────────────────────────────────┐          │
│  │ 近 5 日量价走势迷你图（sparkline）      │          │
│  └───────────────────────────────────────┘          │
└─────────────────────────────────────────────────────┘
```

#### 主线板块卡片

```
┌──────────────────────────┐
│  AI算力          Top 1   │
│  score: 82  ████████░░   │
│  ┌────────┐              │
│  │ rising │  ↑ 第3天     │
│  └────────┘              │
│  龙头：海康威视 +2.3%     │
│  涨停：5 只  连板：2 只   │
└──────────────────────────┘
```

---

### 数据接口格式

前端从以下 JSON 文件读取数据（后端每次运行后更新）：

```
output/{YYYYMMDD}/
├── evaluation.json          每日总览数据
├── radar.json               雷达数据
├── positions.json           持仓数据
├── signal_eval.json         回测数据
└── status.json              系统状态
```

**evaluation.json 结构：**

```json
{
  "date": "2026-05-16",
  "market_risk": {
    "regime": "risk_on",
    "breadth": 0.65,
    "index_above_ma20": true
  },
  "mainline_top5": [
    {
      "sector_name": "AI算力",
      "mainline_score": 82,
      "mainline_status": "rising",
      "rank": 1,
      "limit_up_count": 5,
      "lianban_count": 2,
      "leader": "海康威视"
    }
  ],
  "focus_pool": [
    {
      "symbol": "002415.SZ",
      "name": "海康威视",
      "sector": "AI算力",
      "role": "leader",
      "stage": "stage_2_rising",
      "current_price": 38.50,
      "pct_chg": 2.35,
      "buy_grade": "A",
      "buy_score": 85,
      "strategy_name": "leader_breakout",
      "action": "buy",
      "action_text": "突破确认，板块共振，可半仓介入",
      "confidence": 0.78,
      "data_quality": "full",
      "entry_price_low": 37.50,
      "entry_price_high": 38.80,
      "stop_loss_price": 36.50,
      "position_pct": 0.15,
      "sell_urgency": "无",
      "risk_warnings": [],
      "trend": {
        "state": "strong_up",
        "ma_alignment": "多头排列",
        "slope_20": 0.032
      },
      "volume_price": {
        "volume_ratio": 1.8,
        "status": "healthy"
      },
      "sector_detail": {
        "sector_score": 82,
        "mainline_status": "rising"
      },
      "deny_result": "pass",
      "llm_review": "分歧后放量突破，主升延续概率高"
    }
  ],
  "sell_signals": [],
  "stage_denied": [],
  "observation_pool": []
}
```

**radar.json 结构：**

```json
{
  "date": "2026-05-16",
  "mainline_radar": [
    {
      "sector_name": "固态电池",
      "radar_score": 72,
      "signal_type": "limit_up_cluster",
      "stage_filter": "stage_1",
      "reason": ["板块内出现3只涨停", "成交额放大80%"],
      "suggested_watch": [
        {"symbol": "002460.SZ", "name": "赣锋锂业", "probability": 0.82},
        {"symbol": "300750.SZ", "name": "宁德时代", "probability": 0.75}
      ]
    }
  ],
  "risk_warnings": [
    {
      "target": "002415.SZ",
      "target_type": "stock",
      "level": "caution",
      "signal_type": "leader_decay",
      "reason": ["放量滞涨", "volume_ratio 1.8 但涨幅仅 0.3%"],
      "suggested_action": "提高警惕"
    }
  ]
}
```

---

### 响应式要求

- 主要适配：1920×1080 和 2560×1440（桌面全屏）
- 次要适配：1366×768（笔记本）
- 不需要移动端适配（这是桌面专用工具）

### 无障碍

- 所有颜色编码同时用形状/图标区分（色盲友好）
- A 档 = 绿色 + ✓ 图标
- B 档 = 琥珀 + ◐ 图标
- 卖出 = 红色 + ⚠ 图标
- 键盘可导航

### 性能

- 首屏 < 1 秒
- 50 只股票的列表渲染 < 100ms
- JSON 数据加载异步，有骨架屏

---

### 不要做的事

- 不要做成通用股票软件的样子（不需要搜索框、不需要自选股列表、不需要新闻流）
- 不要用消费级 App 的大圆角、大间距、大图标
- 不要用亮白背景（这是长时间盯屏的工具）
- 不要在首页放太多图表（数据密度 > 可视化花哨）
- 不要用 echarts（太重），用 recharts 或 lightweight-charts
- 不要做登录页（单用户本地系统）

### 要做的事

- 信息密度高但不混乱（靠层次和留白控制）
- 颜色有意义（每种颜色对应一种状态，不装饰性用色）
- 关键数字大而清晰（价格、涨跌幅、评分）
- 次要信息可折叠/hover 展开
- 状态变化有微妙动效（数据刷新脉冲、新信号出现光晕）
- 整体感觉：精密、冷静、专业、值得信赖
